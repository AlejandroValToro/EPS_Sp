meta {
  name: 1. Login
  type: http
  seq: 1
}

post {
  url: http://localhost:5000/auth/login
  body: json
  auth: none
}

body:json {
  {
    "user_id": "admin"
  }
}

script:post-response {
  bru.setEnvVar("token", res.body.token);
}
